import React from 'react';
import {
  Toolbar
} from 'react95';
import styled from 'styled-components';

const CoreToolbar = styled(Toolbar)`
  display: flex;
  min-height: 35px;
  padding-left: 0px;
`;

export default CoreToolbar;
